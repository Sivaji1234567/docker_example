name: Build Docker

on:
  push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Show Files
        run: ls -la

      - name: Build Docker Image
        run: docker build -t fastapi:test .