steps:
  - uses: actions/checkout@v4

  - name: Show Files
    run: ls -la

  - name: Check Python
    run: python --version

  - name: Install Dependencies
    run: pip install -r requirements.txt